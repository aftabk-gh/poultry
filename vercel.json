{
  "builds": [
    {
      "src": "project/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "project/wsgi.py"
    }
  ],
  "build": {
    "command": "npm i && npm build && python manage.py collectstatic --noinput && python manage.py migrate"
  }
}
